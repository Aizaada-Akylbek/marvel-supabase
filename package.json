{
  "name": "test-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "seed:firestore": "node scripts/seed-firestore.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.1"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0"
  }
}
